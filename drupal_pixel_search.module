<?php

/**
 * Implements hook_page_attachments().
 * @param array $attachments
 */
function drupal_pixel_search_page_attachments(array &$attachments) {
  //add here any conditions if you need to limit the pages

  $attachments['#attached']['library'][] = 'drupal_pixel_search/pixel_search_library';
}
